# 2026-02-17

## Durable updates

- 完成 Obsidian → Notion 单向执行流落地：仅同步任务字段（Task, Due, Owner, Project, Priority, Status, Source），不同步全文笔记，不做反向全文同步。
- 已从 Obsidian 复盘文件抽取并导出 CSV：`/Users/a001/Documents/Obsidian Vault/00-Daily/Exports/notion_tasks.csv`。
- 已在 Notion 成功导入该 CSV，导入结果为 3 条任务，字段映射完成并可在表格中查看。
- 当前导入落地在新建数据库（后续可按需统一命名为 `Execution Inbox` 并配置视图）。

## Notes for future runs

- Notion 导入时可走「与 CSV 合并」流程：先上传文件，再进入列类型映射，确认后执行导入。
- 浏览器自动化上传 CSV 的稳定方式：先点击“选择文件”按钮，再调用 upload；不要直接把 upload 绑定到非 input 元素。
